অবশ্যই! আপনার প্রজেক্টের প্রতিটি ফাইল, ফোল্ডার স্ট্রাকচার এবং কাজের প্রবাহ (Workflow) বিস্তারিতভাবে ব্যাখ্যা করে একটি প্রফেশনাল **`README.md`** ফাইল নিচে তৈরি করে দেওয়া হলো।

এটি কপি করে আপনার `d-server/README.md` ফাইলে পেস্ট করুন।

---

```markdown
# ⚡ MeterNet Distributed Automation System (v2.0)

**MeterNet** হলো একটি ডিস্ট্রিবিউটেড ওয়েব অটোমেশন সিস্টেম, যা মূলত REB/PBS এর মিটার পোস্টিং, ইনভেন্টরি ম্যানেজমেন্ট এবং অটোমেশন কাজগুলোকে সহজ করার জন্য তৈরি করা হয়েছে। এটি একটি **মাস্টার সার্ভার** এবং একাধিক **ওয়ার্কার নোড** (Client PC) এর সমন্বয়ে গঠিত।

---

## 📂 প্রোজেক্ট স্ট্রাকচার ও ফাইলের বিবরণ

নিচে পুরো প্রজেক্টের ফাইল স্ট্রাকচার এবং প্রতিটি ফাইলের কাজ বর্ণনা করা হলো:

```plaintext
d-server/
│
├── public/                  # ফ্রন্টএন্ড এবং ডকুমেন্টেশন ফাইল
│   ├── index.html           # লগইন পেজ
│   ├── dashboard.html       # অ্যাডমিন ড্যাশবোর্ড (Live Monitor)
│   ├── api-docs.html        # API ব্যবহারের নিয়মাবলী
│   └── script-dev-guide.html # ডেভেলপারদের জন্য স্ক্রিপ্ট গাইড
│
├── scripts/                 # অটোমেশন স্ক্রিপ্টস (যা ওয়ার্কাররা ডাউনলোড করে)
│   └── rebpbs.js            # REB/PBS অটোমেশনের মূল লজিক ফাইল
│
├── src/                     # ব্যাকএন্ড সোর্স কোড
│   ├── config/
│   │   └── db.js            # MongoDB ডাটাবেস কানেকশন সেটআপ
│   │
│   ├── models/              # ডাটাবেস স্কিমা (Schema)
│   │   ├── Node.js          # ওয়ার্কার/মেশিন এর তথ্য রাখার মডেল
│   │   └── Job.js           # কাজের (Task) তথ্য ও স্ট্যাটাস রাখার মডেল
│   │
│   ├── services/            # বিজনেস লজিক (সিস্টেমের মস্তিষ্ক)
│   │   ├── authService.js       # ওয়ার্কার ভেরিফিকেশন এবং লগইন হ্যান্ডেল করে
│   │   ├── queueService.js      # কাজগুলো (Jobs) কিউ ম্যানেজ ও ডিস্ট্রিবিউশন করে
│   │   ├── pointService.js      # ওয়ার্কারদের রিওয়ার্ড পয়েন্ট ও বোনাস হিসাব করে
│   │   ├── cleanupService.js    # পুরনো কাজ ও ডেড ওয়ার্কার রিমুভ করে
│   │   ├── githubService.js     # গিটহাব থেকে অটোমেটিক স্ক্রিপ্ট আপডেট করে
│   │   └── externalSyncService.js # মেইন সার্ভারে পয়েন্ট সিঙ্ক করে
│   │
│   └── sockets/
│       └── socketHandler.js     # রিয়েল-টাইম কমিউনিকেশন (Socket.io) হ্যান্ডেল করে
│
├── server.js                # মেইন এন্ট্রি পয়েন্ট (Server Entry Point)
├── .env                     # কনফিগারেশন ভেরিয়েবল (Secret Keys)
└── package.json             # ডিপেন্ডেন্সি লিস্ট

```

---

## ⚙️ সিস্টেম ওয়ার্কফ্লো (কিভাবে কাজ করে?)

সিস্টেমটি কিভাবে একটি কাজ (Task) সম্পন্ন করে, তার ধাপগুলো নিচে দেওয়া হলো:

### ১. ওয়ার্কার কানেকশন (Authentication)

* **ফাইল:** `src/services/authService.js`
* যখন কোনো ওয়ার্কার পিসি কানেক্ট হয়, সার্ভার তার `API Key` যাচাই করে।
* সঠিক হলে তাকে ডাটাবেসে `status: 'online'` করে এবং একটি সেশন শুরু করে।

### ২. কাজ তৈরি (Job Creation)

* **ফাইল:** `server.js` (Route: `/app/:scriptName`)
* ড্যাশবোর্ড বা অ্যাপ থেকে কোনো রিকোয়েস্ট আসলে সেটি `Job` মডেলে সেভ হয়।
* স্ট্যাটাস থাকে `queued` (অপেক্ষমান)।

### ৩. কাজ বণ্টন (Job Dispatch)

* **ফাইল:** `src/services/queueService.js`
* `tryDispatch()` ফাংশনটি চেক করে কোন ওয়ার্কার এখন `idle` (বসে আছে)।
* কাজ পেলে সেটি ওয়ার্কারকে পাঠিয়ে দেয় এবং ওয়ার্কারের স্ট্যাটাস `busy` করে দেয়।

### ৪. কাজ সম্পাদন ও ফলাফল (Execution & Completion)

* **ফাইল:** `src/sockets/socketHandler.js` & `src/services/queueService.js`
* ওয়ার্কার কাজটি শেষ করে `task_completed` ইভেন্ট পাঠায়।
* সার্ভার ফলাফল সেভ করে এবং `handleTaskCompletion()` ফাংশন কল করে।
* সফল হলে `pointService.js` এর মাধ্যমে পয়েন্ট যোগ হয়।

### ৫. রক্ষণাবেক্ষণ ও ক্লিনআপ (Maintenance)

* **ফাইল:** `src/services/cleanupService.js`
* প্রতি ১ মিনিট পর পর এটি চেক করে কোনো ওয়ার্কার পিসি ক্র্যাশ করেছে কি না (Zombie Worker)।
* ২ মিনিটের বেশি সাড়া না পেলে তাকে `offline` করে দেয় এবং ১ ঘন্টার পুরনো ডাটা ডিলিট করে।

---

## 🚀 ফিচারসমূহ

1. **স্মার্ট কিউ ম্যানেজমেন্ট:** কাজগুলো সিরিয়াল অনুযায়ী এবং লোড ব্যালেন্সিং করে ওয়ার্কারদের দেওয়া হয়।
2. **অটোমেটিক স্ক্রিপ্ট আপডেট:** ড্যাশবোর্ড থেকে এক ক্লিকে গিটহাব থেকে নতুন স্ক্রিপ্ট নামানো যায় (`githubService.js`)।
3. **বোনাস পয়েন্ট সিস্টেম:** প্রতি ১৫ মিনিট অনলাইনে থাকলে বা কাজ করলে অটোমেটিক বোনাস পয়েন্ট পায় (`pointService.js`)।
4. **ডেড ওয়ার্কার ডিটেকশন:** পিসি হঠাৎ বন্ধ হলে সিস্টেম তা বুঝে নেয় এবং ডাটাবেস আপডেট করে (`cleanupService.js`)।
5. **মাল্টি-নোড সাপোর্ট:** একই ইউজার একাধিক পিসি দিয়ে কাজ করতে পারে।

---

## 🛠️ সেটআপ গাইড

### ১. পরিবেশন ভেরিয়েবল (.env)

প্রজেক্টের রুটে `.env` ফাইল তৈরি করে নিচের তথ্য দিন:

```env
PORT=8000
MONGO_URI=mongodb://localhost:27017/meternet
PBSNET_WORKER_URL=[https://your-worker-url.workers.dev](https://your-worker-url.workers.dev)
PBSNET_ADMIN_SECRET=YourSecretKey
DASHBOARD_PASSWORD=admin123
GITHUB_REPO_URL=[https://github.com/your-repo/d-script](https://github.com/your-repo/d-script)

```

### ২. ইন্সটলেশন

টার্মিনালে কমান্ড দিন:

```bash
npm install

```

### ৩. সার্ভার চালু করা

```bash
node server.js

```

অথবা ডেভেলপমেন্ট মোডে:

```bash
npm run dev

```

---

## 🔍 API এন্ডপয়েন্টসমূহ

| Method | Endpoint | বিবরণ |
| --- | --- | --- |
| `POST` | `/app/:scriptName` | নতুন কাজ (Job) সাবমিট করা |
| `GET` | `/api/dashboard-data` | ড্যাশবোর্ডের লাইভ ডাটা দেখা |
| `GET` | `/api/status/:id` | নির্দিষ্ট কাজের স্ট্যাটাস চেক করা |
| `POST` | `/api/admin/sync-github` | স্ক্রিপ্ট আপডেট করা |
| `POST` | `/api/admin/clear-history` | পুরনো হিস্ট্রি মুছে ফেলা |

---

**বিকাশকারী:** [Salman Bappy](https://www.google.com/search?q=https://github.com/salmanbappy467)
**ভার্সন:** ২.০.০ (স্টেবল)

```

```